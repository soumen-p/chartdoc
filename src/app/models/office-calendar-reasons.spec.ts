import { OfficeCalendarReasons } from './office-calendar-reasons';

describe('OfficeCalendarReasons', () => {
  it('should create an instance', () => {
    expect(new OfficeCalendarReasons()).toBeTruthy();
  });
});
